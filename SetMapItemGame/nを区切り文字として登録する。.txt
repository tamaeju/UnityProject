\nを区切り文字として登録する。
↓
文字列の配列に入れる。
↓
文字列の配列を要素番号ごとに確認し、次の配列と足して半角44文字以内であれば、次の配列と合成する。

limitcount = 44;
for(int i = 0;i<length;i++){
if((s[i].length+s[i+1].length)<limitcount)
s[i] = s[i]+s[i+1];
Remove(s[i+1]);
}


↓
最終的に、区切り位置で\nをつけて出し直す。